services:
  app:
  build: .
    image: django-ecommerce
    networks:
      - web

  nginx:
    image: nginx:latest
    ports:
      - "81:81"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
    networks:
      - web

networks:
  web:
