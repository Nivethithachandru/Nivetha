services:
  app:
    image: nivekrish/django-ecommerce:latest
    networks:
      - web

  nginx:
    image: nginx:latest
    ports:
      - "81:81"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./static:/app/staticfiles:ro 
    depends_on:
      - app
    networks:
      - web

networks:
  web:
