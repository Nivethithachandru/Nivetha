services:
  app:
    image: django-ecommerce:latest
    networks:
      - web

  nginx:
    image: nginx:latest
    ports:
      - "81:81"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
    networks:
      - web

networks:
  web:
